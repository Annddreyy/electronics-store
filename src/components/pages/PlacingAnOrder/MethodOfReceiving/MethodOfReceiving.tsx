import classes from './../PlacingAnOrder.module.scss';

export const MethodOfReceiving: React.FC = () => {
    return (
        <section className={classes.orderBlock}>
            <form action="">
                <h2 className={classes.blockTitle}>Способ получения</h2>
            </form>
        </section>
    );
};
